import { Book, ReadingStatus } from '@app/backend';\n\nexport interface SummaryStats {\n  totalBooks: number;\n  byStatus: Record<ReadingStatus, number>;\n  totalPages?: number;\n  totalPagesCompleted?: number;\n  completionRate: number; // between 0 and 1\n  currentlyReading: number;\n  topAuthors: { author: string; count: number }[];\n  firstAddedAt?: string;\n  lastAddedAt?: string;\n}\n\nexport type { Book, ReadingStatus };\n